git add . // 							(Subir todos los archivos que esten en la ruta donde estamos.)

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

git commit -m "Comentario sobre el cambio que hicimos" // 	(Mensaje para dar información sobre que lo que hicimos)

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

git push -u origin main // 					(Este comando sube (envía) los cambios locales del repositorio hacia un repositorio remoto (como GitHub )